{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "node --import tsx src/calendar-broker/server.ts",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
