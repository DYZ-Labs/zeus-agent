node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# Zeus's memory must never be committed. The real store lives in ~/.zeus,
# but temp/test databases can land here — refuse all of them.
*.db
*.db-wal
*.db-shm
*.sqlite
.zeus/

.env
.env.local
.DS_Store

# Local embedding model cache
.models/
